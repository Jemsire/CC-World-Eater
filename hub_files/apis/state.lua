user_input = {}
turtles = {}
pockets = {}
homes = {}

-- Create state table to reference all globals (for compatibility with code that uses state.*)
state = {
    user_input = user_input,
    turtles = turtles,
    pockets = pockets,
    homes = homes
}

